#pragma

#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>

namespace Safaga
{
	namespace MathUtils
	{
		// 
		typedef glm::vec3 vec3;
		typedef glm::vec4 vec4;
		typedef glm::mat4 mat4;

		//// Methods
		//template <typename... Args>
		//auto lookAt(Args&&... args) -> decltype(f(std::forward<Args>(args)...)) {
		//	return  glm::lookAt(std::forward<Args>(args)...);
		//}

		//template <typename... Args>
		//auto normalize(Args&&... args) -> decltype(f(std::forward<Args>(args)...)) {
		//	return  glm::normalize(std::forward<Args>(args)...);
		//}

		//template <typename... Args>
		//auto radians(Args&&... args) -> decltype(f(std::forward<Args>(args)...)) {
		//	return  glm::radians(std::forward<Args>(args)...);
		//}

		//template <typename... Args>
		//auto perspective(Args&&... args) -> decltype(f(std::forward<Args>(args)...)) {
		//	return  glm::perspective(std::forward<Args>(args)...);
		//}
	}
}
